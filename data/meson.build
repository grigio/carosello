install_data(
  base_id + '.desktop',
  install_dir: datadir / 'applications',
)

install_data(
  base_id + '.metainfo.xml',
  install_dir: datadir / 'metainfo',
)

install_subdir(
  'icons',
  install_dir: datadir,
)
